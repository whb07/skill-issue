interface:
  display_name: "Cursor Agent CLI"
  short_description: "Run Cursor Agent safely from scripts"
  default_prompt: "Use $agent-cli with its default Grok 4.5 model to run Cursor Agent headlessly for this task."
